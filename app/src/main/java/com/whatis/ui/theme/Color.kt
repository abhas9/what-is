package com.whatis.ui.theme

import androidx.compose.ui.graphics.Color

// Kid-friendly bright colors - Primary palette (WCAG AA compliant)
val BrightBlue = Color(0xFF1565C0)       // Darker blue for better contrast
val SunnyOrange = Color(0xFFDD2C00)      // Darker orange for better contrast  
val VibrantGreen = Color(0xFF2E7D32)     // Darker green for better contrast

// Kid-friendly bright colors - Secondary palette
val CheerfulYellow = Color(0xFFF57F17)   // Darker yellow for better contrast
val PlayfulPurple = Color(0xFF9C27B0)    // Already good contrast
val CoralPink = Color(0xFFBF360C)        // Darker coral for better contrast

// Background and surface colors
val LightCream = Color(0xFFFFFEF7)
val SoftSkyBlue = Color(0xFFE3F2FD)
val PureWhite = Color(0xFFFFFFFF)

// Light container colors for better visual hierarchy
val BrightBlueLight = Color(0xFFBBDEFB)
val SunnyOrangeLight = Color(0xFFFFE0B2)
val VibrantGreenLight = Color(0xFFC8E6C9)

// Darker variants for better contrast
val BrightBlueDark = Color(0xFF0D47A1)
val SunnyOrangeDark = Color(0xFFBF360C)
val VibrantGreenDark = Color(0xFF1B5E20)

// Text colors
val OnLightBackground = Color(0xFF1C1B1F)
val OnDarkBackground = Color(0xFFE6E1E5)
val OnPrimaryColors = Color(0xFFFFFFFF)